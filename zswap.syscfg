## Compression algorithm: deflate, lz4hc, lzo, lzo-rle, 842, zstd (default)
#compressor=zstd

## Pool allocator (objects per page): z3fold - 3, zbud - 2 (default)
zpool=z3fold

## Memory allocated for pool, % (default 20)
#max_pool_percent=25

## Accept threshold, % (default 90)
#accept_threshold_percent=90
